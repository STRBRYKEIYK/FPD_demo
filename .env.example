# Frontend OCR integration
# Base URL for ingestion-service OCR endpoints used by the finance document flow.
VITE_INGESTION_API_BASE_URL=http://localhost:4100

# Toggle mock OCR mode in frontend.
# false = call real ingestion-service OCR
# true  = return demo mock OCR data in frontend only
VITE_USE_MOCK_OCR=false
